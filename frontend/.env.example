VITE_API_BASE_URL=http://127.0.0.1:8000

# When 'true', the app uses the in-browser mock API (seeded from src/lib/mock/test_data.json).
# When 'false', the app talks to the real Laravel backend.
VITE_USE_MOCK=true
